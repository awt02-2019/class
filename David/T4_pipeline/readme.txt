Lab 1 runs with "sudo find /var/log/ -name *.log"
Lab 2 poll SCM execute if a new commit is made and check every 5 minutes
Lab pipeline A Jenkinsfile was uploaded but still needs work

phpunit add to composer
command run with ./vendor/bin/phpunit and show a primarly passed test
